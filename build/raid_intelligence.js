/* War Room v1.7 - TBC Phase 3 Raid Intelligence */
const TBC_PHASE3={raidSize:25,groupSize:5,targets:{Tank:{min:2,max:4},Healer:{min:5,max:8},Melee:{min:4,max:10},Ranged:{min:7,max:14}}};
const SPEC_SYNERGY={Shaman:{Enhancement:['Melee'],Elemental:['Ranged'],Restoration:['Healer','Ranged']},Druid:{Feral:['Melee','Tank'],Balance:['Ranged'],Restoration:['Healer']},Hunter:{'Beast Mastery':['Ranged'],Marksmanship:['Ranged'],Survival:['Ranged']},Mage:{Arcane:['Ranged'],Fire:['Ranged'],Frost:['Ranged']},Paladin:{Holy:['Healer'],Protection:['Tank'],Retribution:['Melee']},Priest:{Discipline:['Healer'],Holy:['Healer'],Shadow:['Ranged']},Rogue:{Assassination:['Melee'],Combat:['Melee'],Subtlety:['Melee']},Warlock:{Affliction:['Ranged'],Demonology:['Ranged'],Destruction:['Ranged']},Warrior:{Arms:['Melee'],Fury:['Melee'],Protection:['Tank']}};
const WR_ENCOUNTERS={'Black Temple':{'Mother Shahraz':{survivability:1.35,healing:1.15},'The Illidari Council':{utility:1.35,healing:1.1},'Illidan Stormrage':{tanks:1.35,healing:1.2,utility:1.25,damage:1.1}},'Battle for Mount Hyjal':{'Archimonde':{survivability:1.4,healing:1.15,damage:1.05}}};
function wrNorm(v){return String(v||'').trim()} function wrRole(p){return wrNorm(p.role)||'Unknown'} function wrSpec(p){return wrNorm(p.spec||p.talentSpec||p.build)} function wrClass(p){return wrNorm(p.class||p.className)}
function wrFlattenGroups(groups){if(!Array.isArray(groups))return[];return groups.flatMap((g,gi)=>{let list=Array.isArray(g)?g:(g&&Array.isArray(g.players)?g.players:[]);return list.filter(Boolean).map(p=>({...p,_group:gi+1}))})}
function wrDomPlayers(doc){if(!doc||!doc.querySelectorAll)return[];let groups=[...doc.querySelectorAll('.group,[data-group],[data-raid-group]')].slice(0,5),out=[];groups.forEach((g,gi)=>{let cards=[...g.querySelectorAll('.player-card,.raid-player,[data-player],[data-name]')];cards.forEach(card=>{let d=card.dataset||{},txt=wrNorm(card.textContent),classes=[...(card.classList||[])].map(String);let cls=wrNorm(d.class||d.playerClass||d.className);if(!cls){cls=['Druid','Hunter','Mage','Paladin','Priest','Rogue','Shaman','Warlock','Warrior'].find(x=>classes.some(c=>c.toLowerCase().includes(x.toLowerCase()))||txt.includes(x))||''}let role=wrNorm(d.role||d.playerRole);if(!role){role=['Tank','Healer','Melee','Ranged'].find(x=>classes.some(c=>c.toLowerCase().includes(x.toLowerCase()))||txt.includes(x))||''}let name=wrNorm(d.name||d.playerName);if(!name){let el=card.querySelector&&card.querySelector('.name,.player-name,.char-name,strong');name=wrNorm(el?el.textContent:'')}let spec=wrNorm(d.spec||d.talentSpec||d.build);if(name||cls)out.push({name:name||cls,class:cls,spec,role,_group:gi+1})})});return out}
function wrRaidPlayers(){let candidates=[window.raid,window.raidGroups,window.groups,window.raidState&&window.raidState.groups,window.state&&window.state.raid,window.appState&&window.appState.raid];for(let c of candidates){let p=wrFlattenGroups(c);if(p.length)return p}let dom=wrDomPlayers(typeof document!=='undefined'?document:null);if(dom.length)return dom;return[]}
function wrCountByRole(ps){return ps.reduce((a,p)=>{let r=wrRole(p);a[r]=(a[r]||0)+1;return a},{})}
function wrPartySynergy(ps){let roles=wrCountByRole(ps),melee=(roles.Melee||0)+(roles.Tank||0),casters=(roles.Ranged||0)+(roles.Healer||0),score=0;for(let p of ps){let c=wrClass(p),s=wrSpec(p);if(c==='Shaman'&&s==='Enhancement')score+=Math.min(melee,4)*3;if(c==='Shaman'&&s==='Elemental')score+=Math.min(roles.Ranged||0,4)*3;if(c==='Shaman'&&s==='Restoration')score+=Math.min(casters,4)*2;if(c==='Priest'&&s==='Shadow')score+=Math.min(casters,4)*2;if(c==='Druid'&&s==='Feral')score+=Math.min(melee,4)*2;if(c==='Druid'&&s==='Balance')score+=Math.min(roles.Ranged||0,4)*2;if(c==='Hunter'&&s==='Marksmanship')score+=Math.min(roles.Ranged||0,4);if(c==='Paladin'&&s==='Retribution')score+=Math.min(melee,4)}return score}
function wrPartyAnalysis(ps){return[1,2,3,4,5].map(n=>{let players=ps.filter(p=>p._group===n),roles=wrCountByRole(players),shamans=players.filter(p=>wrClass(p)==='Shaman'),notes=[];if(players.length<5)notes.push(`${5-players.length} open slot${5-players.length===1?'':'s'}`);if(players.length===5&&!shamans.length)notes.push('No Shaman party support');let melee=(roles.Melee||0)+(roles.Tank||0);shamans.forEach(s=>{let sp=wrSpec(s);if(sp==='Enhancement'&&melee<3)notes.push('Enhancement Shaman has low melee value in this party');if(sp==='Elemental'&&(roles.Ranged||0)<2)notes.push('Elemental Shaman has low caster value in this party')});return{group:n,players,roles,notes,synergy:wrPartySynergy(players)}})}
function wrCoverage(ps){let c=new Set(ps.map(wrClass)),s=new Set(ps.map(p=>`${wrClass(p)}:${wrSpec(p)}`));return[['Paladin Blessings',c.has('Paladin')],['Power Word: Fortitude',c.has('Priest')],['Mark of the Wild',c.has('Druid')],['Arcane Intellect',c.has('Mage')],['Healthstones',c.has('Warlock')],['Shaman Party Support',c.has('Shaman')],['Bloodlust / Heroism',c.has('Shaman')],['Shadow Priest Mana Support',s.has('Priest:Shadow')],['Totem of Wrath',s.has('Shaman:Elemental')],['Windfury / Melee Totems',s.has('Shaman:Enhancement')]].map(([name,ok])=>({name,ok}))}
function wrEncounter(){let raid=window.wrSelectedRaid||'Black Temple',boss=window.wrSelectedBoss||'Illidan Stormrage';return{raid,boss,weights:(WR_ENCOUNTERS[raid]||{})[boss]||{}}}
function wrSwapCandidates(parties){let candidates=[];for(let i=0;i<parties.length;i++)for(let j=i+1;j<parties.length;j++){let a=parties[i],b=parties[j],before=a.synergy+b.synergy;for(let pa of a.players)for(let pb of b.players){let na=a.players.map(x=>x===pa?pb:x),nb=b.players.map(x=>x===pb?pa:x),gain=wrPartySynergy(na)+wrPartySynergy(nb)-before;if(gain>=4)candidates.push({gain,a:a.group,b:b.group,pa,pb,text:`Swap ${pa.name||wrClass(pa)} (G${a.group}) with ${pb.name||wrClass(pb)} (G${b.group}) — projected party-synergy gain +${gain}.`})}}return candidates.sort((x,y)=>y.gain-x.gain).slice(0,4)}
function wrRaidIntelligence(){let players=wrRaidPlayers(),roles=wrCountByRole(players),parties=wrPartyAnalysis(players),coverage=wrCoverage(players),warnings=[],recommendations=[],encounter=wrEncounter();if(!players.length)warnings.push('Raid Intelligence is waiting for live raid-group data.');if(players.length&&players.length<25)warnings.push(`Raid is ${25-players.length} player${25-players.length===1?'':'s'} short of 25.`);if((roles.Tank||0)<2)warnings.push('Tank coverage is below the normal 25-player baseline.');if((roles.Healer||0)<5)warnings.push('Healing coverage is below the normal 25-player baseline.');parties.forEach(g=>g.notes.forEach(n=>warnings.push(`Group ${g.group}: ${n}`)));coverage.filter(x=>!x.ok).forEach(x=>warnings.push(`Missing coverage: ${x.name}`));let swaps=wrSwapCandidates(parties);recommendations.push(...swaps.map(x=>x.text));let filled=Math.min(players.length/25,1),roleHealth=Math.min(((roles.Tank||0)>=2?1:0)+((roles.Healer||0)>=5?1:0)+((roles.Ranged||0)>=7?1:0),3)/3,coverageHealth=coverage.filter(x=>x.ok).length/coverage.length,base=filled*.4+roleHealth*.3+coverageHealth*.3;let modifier=1;if(encounter.weights.tanks&&((roles.Tank||0)<3))modifier-=.04*(encounter.weights.tanks-1);if(encounter.weights.healing&&((roles.Healer||0)<6))modifier-=.05*(encounter.weights.healing-1);if(encounter.weights.utility&&coverageHealth<.8)modifier-=.08*(encounter.weights.utility-1);let score=Math.max(0,Math.min(100,Math.round(base*modifier*100)));return{score,players,roles,parties,coverage,warnings,recommendations,swaps,encounter,method:'heuristic'}}
window.WarRoomRaidIntelligence={analyze:wrRaidIntelligence,extractPlayers:wrRaidPlayers,extractFromDOM:wrDomPlayers,rules:TBC_PHASE3,specSynergy:SPEC_SYNERGY,encounters:WR_ENCOUNTERS,setEncounter:(raid,boss)=>{window.wrSelectedRaid=raid;window.wrSelectedBoss=boss;if(window.renderRaidIntelligence)window.renderRaidIntelligence()}};
