 async function enrichLiveItem(item){if(!item||item.__wrLiveResolved)return item;item.__wrLiveResolved=true;const gems=Array.isArray(item.gems)?item.gems:[],enchants=Array.isArray(item.enchantments)?item.enchantments:[];await Promise.all(gems.map(async g=>{if(!g||!g.id)return;const j=await resolveLive('gem',g.id);if(j){g.name=g.name||j.name;g.text=g.text||j.tooltip||j.name;g.icon=g.icon||j.icon;g.url=g.url||j.url;g.tooltip=g.tooltip||j.tooltip}}));await Promise.all(enchants.map(async e=>{if(!e||!e.id)return;const j=await resolveLive('enchantment',e.id);if(j){e.name=e.name||j.name;e.text=e.text||j.effect||j.name;e.effect=e.effect||j.effect}}));if(enchants.length)item.enchant=enchants.map(e=>norm(e.text||e.effect||e.name)).filter(Boolean).join('; ');return item}
 function ensureTooltip(){if(tooltip?.isConnected)return tooltip;tooltip=document.createElement('div');tooltip.className='wr-wow-tooltip';tooltip.style.cssText='position:fixed;z-index:2147483646;display:none;pointer-events:none;max-width:390px;min-width:270px;padding:12px 14px;border:1px solid #7b6a4a;border-radius:4px;background:linear-gradient(180deg,rgba(12,10,18,.98),rgba(4,4,8,.98));box-shadow:0 0 0 1px #000,0 8px 30px rgba(0,0,0,.8);font-family:Arial,sans-serif;font-size:13px;line-height:1.32;color:#fff;text-align:left;white-space:normal';document.body.appendChild(tooltip);return tooltip}
 function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
 function first(item,names){for(const n of names){const v=item?.[n];if(v!==undefined&&v!==null&&v!=='')return v}return null}
 function statLines(item){const out=[],stats=first(item,['stats','statValues','attributes']);if(Array.isArray(stats))for(const s of stats){if(typeof s==='string')out.push(s);else if(s&&typeof s==='object')out.push(norm(s.text||s.label||s.name)+(s.value!=null?' '+s.value:''))}else if(stats&&typeof stats==='object')for(const [k,v] of Object.entries(stats))if(v!=null&&v!==0)out.push(`${v>0?'+':''}${v} ${k.replace(/_/g,' ')}`);return out.filter(Boolean)}
 function effectLines(item){const vals=[];for(const k of ['effects','equipEffects','spells','tooltip','description']){const v=item?.[k];if(Array.isArray(v))for(const x of v)vals.push(typeof x==='string'?x:(x?.text||x?.description||x?.name||''));else if(typeof v==='string')vals.push(v)}return [...new Set(vals.map(norm).filter(Boolean))]}
 function gemHtml(g){if(typeof g==='string')return `<div style="color:#9d9d9d">${esc(g)}</div>`;const text=norm(g?.text||g?.tooltip||g?.name||g?.socketColor||'Socket'),src=norm(g?.url)||iconUrl(g?.icon);return `<div style="display:flex;align-items:flex-start;gap:6px;color:#9d9d9d;margin-top:2px">${src?`<img src="${esc(src)}" width="16" height="16" style="width:16px;height:16px;border:1px solid #555;border-radius:2px;object-fit:cover;margin-top:1px">`:''}<span>${esc(text)}</span></div>`}
 function tooltipHtml(item,label){const q=norm(item?.quality).toLowerCase(),color=qualityColor[q]||'#fff',name=esc(item?.name||label);let h=`<div style="font-size:15px;font-weight:700;color:${color};margin-bottom:3px">${name}</div>`;const ilvl=first(item,['ilvl','itemLevel','level']);if(ilvl!=null)h+=`<div style="color:#ffd100;margin-bottom:5px">Item Level ${esc(ilvl)}</div>`;const bind=first(item,['binding','bind','bonding']);if(bind)h+=`<div>${esc(bind)}</div>`;const slot=first(item,['inventoryType','type','slotName','slot'])||label,sub=first(item,['subclass','subType','armorType','weaponType']);h+=`<div style="display:flex;justify-content:space-between;gap:20px"><span>${esc(slot)}</span>${sub?`<span>${esc(sub)}</span>`:''}</div>`;for(const pair of [['armor',['armor']],['damage',['damage','damageText']],['speed',['speed']],['block',['block']]]){const v=first(item,pair[1]);if(v!=null)h+=`<div>${pair[0]==='armor'?esc(v)+' Armor':pair[0]==='speed'?'Speed '+esc(v):esc(v)}</div>`}for(const s of statLines(item))h+=`<div style="color:#fff">${esc(s)}</div>`;const enchantments=Array.isArray(item?.enchantments)?item.enchantments:[];for(const e of enchantments){const text=norm(e?.text||e?.effect||e?.name);if(text)h+=`<div style="color:#1eff00;margin-top:4px">Enchant: ${esc(text)}</div>`}if(!enchantments.length){const enchant=first(item,['enchant','enchantment']);if(enchant)h+=`<div style="color:#1eff00;margin-top:4px">Enchant: ${esc(typeof enchant==='string'?enchant:(enchant?.name||enchant?.description||''))}</div>`}const sockets=first(item,['sockets','gems']);if(Array.isArray(sockets))for(const s of sockets)h+=gemHtml(s);for(const e of effectLines(item))h+=`<div style="color:#1eff00;margin-top:3px">${esc(e)}</div>`;const req=first(item,['requiredLevel','requiresLevel']);if(req!=null)h+=`<div>Requires Level ${esc(req)}</div>`;const durability=first(item,['durability']);if(durability)h+=`<div>Durability ${esc(durability)}</div>`;const set=first(item,['setName','itemSet']);if(set)h+=`<div style="color:#ffd100;margin-top:5px">${esc(typeof set==='string'?set:(set?.name||''))}</div>`;return h}
 function positionTooltip(ev){const t=ensureTooltip(),pad=14,r=t.getBoundingClientRect();let x=ev.clientX+18,y=ev.clientY+18;if(x+r.width+pad>innerWidth)x=ev.clientX-r.width-18;if(y+r.height+pad>innerHeight)y=ev.clientY-r.height-18;t.style.left=Math.max(pad,x)+'px';t.style.top=Math.max(pad,y)+'px'}
